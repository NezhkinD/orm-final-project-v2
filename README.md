# Learning Platform - ORM Final Project

Учебная платформа для демонстрации концепций ORM с использованием Hibernate/JPA и PostgreSQL. Проект включает 15+ JPA сущностей с различными типами связей, полноценный REST API с документацией Swagger, и систему управления курсами, студентами, заданиями и тестами.

## Быстрый запуск

Самый простой способ запустить проект - использовать команду `make start-all`, которая автоматически запустит БД и приложение:

```bash
make start-all
```

Эта команда:
- Запустит PostgreSQL в Docker контейнере
- Запустит Spring Boot приложение
- Покажет все доступные URL

После запуска будут доступны:
- **Swagger UI**: http://localhost:8081/swagger-ui.html
- **OpenAPI Docs**: http://localhost:8081/api-docs
- **Database**: localhost:5432 (postgres/postgres)

Остановка:
```bash
make stop-all
```

## 📋 Основные команды

| Команда | Описание |
|---------|----------|
| `make start-all` | Запустить БД и приложение |
| `make stop-all` | Остановить всё |
| `make status` | Проверить статус приложения |
| `make logs` | Просмотреть логи (tail -f) |
| `make swagger` | Открыть Swagger UI в браузере |
| `make help` | Показать все доступные команды |

## Технологический стек

- **Java**: 21
- **Spring Boot**: 3.1.11
- **Spring Data JPA** / **Hibernate**
- **PostgreSQL**: 15
- **Maven**: 3.9
- **Lombok**: 1.18.30
- **Springdoc OpenAPI**: 2.2.0 (Swagger UI)
- **Docker** & **Docker Compose**

## Требования

### Для запуска с Make (рекомендуется)
- Java 21+
- Maven 3.6+
- Docker & Docker Compose (для БД)
- Make utility

### Для запуска с Docker Compose
- Docker 20.10+
- Docker Compose 2.0+

### Для локальной разработки (без Docker)
- Java 21+
- PostgreSQL 13+
- Maven 3.6+

## 💻 Способы запуска

### Метод 1: Make (Рекомендуется для разработки)

Использует Docker для БД, локальный Maven для приложения:

```bash
# Запустить всё
make start-all

# Проверить статус
make status

# Просмотреть логи
make logs

# Открыть Swagger UI
make swagger

# Остановить всё
make stop-all
```

## REST API и Swagger

### Интерактивная документация API

После запуска приложения доступна полная интерактивная документация через Swagger UI:

- **Swagger UI** (Make): http://localhost:8081/swagger-ui.html
- **Swagger UI** (Docker): http://localhost:8080/swagger-ui.html
- **OpenAPI JSON**: `/v3/api-docs`
- **OpenAPI YAML**: `/v3/api-docs.yaml`

### Возможности Swagger UI

- **86+ REST endpoints** для всех сущностей
- **Интерактивное тестирование** - выполняйте запросы прямо из браузера
- **Автоматическая документация** схем запросов/ответов
- **Примеры** для каждого endpoint
- **Поиск и фильтрация** endpoints

### Основные группы API

| Группа | Описание | Endpoints |
|--------|----------|---|
| **User Management** | Пользователи и профили | 10 |
| **Course Management** | Курсы и структура | 12 |
| **Module Management** | Модули курсов | 8 |
| **Lesson Management** | Уроки | 8 |
| **Enrollment** | Записи на курсы | 6 |
| **Assignment** | Задания и решения | 10 |
| **Quiz Management** | Тесты и вопросы | 15 |
| **Category** | Категории курсов | 6 |
| **Review** | Отзывы и рейтинги | 7 |
| **Tag Management** | Теги курсов | 4 |


## Архитектура проекта

### Структура кода

```
orm-final-project/
├── src/main/java/org/example/learningplatform/
│   ├── entity/          # 15+ JPA сущностей с аннотациями
│   │   ├── User.java
│   │   ├── Course.java
│   │   ├── Module.java
│   │   ├── Quiz.java
│   │   └── ...
│   ├── repository/      # Spring Data JPA репозитории
│   │   └── (extends JpaRepository)
│   ├── service/         # Бизнес-логика (@Transactional)
│   │   ├── impl/        # Реализации сервисов
│   │   └── ...
│   ├── controller/      # REST контроллеры (@RestController)
│   ├── dto/             # Data Transfer Objects
│   ├── exception/       # Обработка ошибок
│   └── config/          # Конфигурация Swagger, JPA
├── src/main/resources/
│   └── application.properties   # Настройки БД, JPA
├── src/test/java/
│   └── integration/     # @SpringBootTest интеграционные тесты
├── Makefile             # Команды для управления проектом
├── docker-compose.yml   # Docker конфигурация
├── Dockerfile           # Multi-stage build
└── pom.xml              # Maven зависимости
```

## Модель данных (15 JPA сущностей)

Проект демонстрирует все типы JPA связей:

### Core Entities

| Сущность | Описание | Основные связи |
|----------|----------|----------------|
| **User** | Пользователи (студенты/учителя) | @OneToOne Profile, @OneToMany Enrollment |
| **Profile** | Профили пользователей | @OneToOne User |
| **Course** | Курсы | @ManyToOne Category, @OneToMany Module |
| **Category** | Категории курсов | @OneToMany Course |
| **Module** | Модули курса | @ManyToOne Course, @OneToMany Lesson |
| **Lesson** | Уроки | @ManyToOne Module |
| **Enrollment** | Записи на курсы | @ManyToOne User, @ManyToOne Course |
| **Assignment** | Задания | @ManyToOne Course, @OneToMany Submission |
| **Submission** | Решения заданий | @ManyToOne Assignment, @ManyToOne User |
| **Quiz** | Тесты | @ManyToOne Course, @OneToMany Question |
| **Question** | Вопросы теста | @ManyToOne Quiz, @OneToMany AnswerOption |
| **AnswerOption** | Варианты ответов | @ManyToOne Question |
| **QuizSubmission** | Результаты тестов | @ManyToOne Quiz, @ManyToOne User |
| **CourseReview** | Отзывы о курсах | @ManyToOne Course, @ManyToOne User |
| **Tag** | Теги | @ManyToMany Course |

### Типы связей

- **@OneToOne**: User ↔ Profile
- **@OneToMany / @ManyToOne**: Course → Module → Lesson (иерархия)
- **@ManyToMany**: Course ↔ Tag (через @JoinTable)
- **Join Entity**: Enrollment (User ↔ Course с метаданными)

### Особенности ORM

- **Lazy Loading**: Все коллекции загружаются лениво (`fetch = LAZY`)
- **Cascade Operations**: Демонстрация каскадного сохранения/удаления
- **Orphan Removal**: Автоматическое удаление зависимых сущностей
- **Custom Queries**: JOIN FETCH для избежания N+1 проблемы
- **@Transactional**: Управление транзакциями в сервисном слое

## Конфигурация

### JPA/Hibernate настройки

```properties
# Auto-create/update database schema
spring.jpa.hibernate.ddl-auto=update

# Show SQL in console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Disable OSIV to demonstrate lazy loading
spring.jpa.open-in-view=false

# Database connection
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
```

## Тестирование

### Запуск тестов

```bash
# Все тесты
make test

# Только интеграционные тесты
make test-integration

# Конкретный тест
mvn test -Dtest=CourseServiceIntegrationTest
```

### Тесты в Docker

```bash
# Запуск в контейнере
docker-compose exec app mvn test

# Интеграционные тесты
docker-compose exec app mvn test -Dtest=**/*IntegrationTest
```

### Что покрывают тесты

- ✅ CRUD операции для всех сущностей
- ✅ Каскадные операции (save parent → child saves)
- ✅ Lazy loading и его решения
- ✅ Валидация данных
- ✅ Обработка ошибок и исключений
- ✅ Custom queries и JOIN FETCH

## Основные функции

### Управление курсами

- Создание/редактирование курсов с категориями
- Иерархическая структура: Course → Module → Lesson
- Теги и поиск по категориям

### Система заданий

- Преподаватели создают задания
- Студенты отправляют решения
- Оценивание и обратная связь

### Система тестирования

- Создание тестов с вопросами и вариантами ответов
- Автоматическая проверка правильных ответов
- Хранение результатов студентов

### Управление пользователями

- Регистрация студентов и преподавателей
- Профили с дополнительной информацией
- Запись на курсы (Enrollment)

### Отзывы и рейтинги

- Студенты оставляют отзывы о курсах
- Система рейтингов (1-5 звезд)
- Расчет средней оценки курса

## Быстрые ссылки

После запуска приложения:

| Ссылка | URL | Описание |
|--------|-----|----------|
| Swagger UI (Make) | http://localhost:8081/swagger-ui.html | Интерактивная документация API |
| Swagger UI (Docker) | http://localhost:8080/swagger-ui.html | Интерактивная документация API |
| OpenAPI JSON | http://localhost:8081/api-docs | Спецификация в формате JSON |
| Health Check | http://localhost:8081/actuator/health | Проверка статуса приложения |